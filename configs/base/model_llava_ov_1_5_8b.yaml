name: llava_ov_1_5_8b
# Use the HF-compatible converted base for local training.
# The raw Hub checkpoint keeps the old `model.*` / `visual.*` text+vision naming,
# while our current LLaVA-OneVision-1.5 remote code expects
# `model.language_model.*` / `model.visual.*`. Training from the raw Hub path
# leaves the entire language tower newly initialized.
hf_path: /mnt/hdd/xuran/vlm_safety_harness/models/llava_ov_1_5_8b_base_hfcompat
architecture: llava_ov_1_5
size_b: 8.0
trust_remote_code: true
max_model_len: 4096
