# Ablation: train only on tool→target pattern samples (no diverse relation types)

name: abl_no_diverse_relations
description: "Ablation: DREAMS training without relation-type diversity (tool→target only)"
tags: [ablation, no-diverse-relations]
group: ablation

_extends: experiments/main/main_dreams_internvl.yaml

dataset:
  name: dreams_tool_target_only
  # Assumes a pre-filtered JSON is generated by scripts/filter_by_relation_type.py
  train_path: /mnt/hdd/xuran/vlm_safety_harness/data_links/our_dataset/train_tool_target_only.json
